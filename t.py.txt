self.STRESSED_MINIMUM_DSCR = str(
    next(
        filter(
            lambda item: item[0] <= self.stress_min <= item[1][0],
            stress.items()
        ),
        None
    )
)[1]

self.AVERAGE_DSCRM = next(
    (dscr for range_start, range_end, dscr in dscrm if range_start <= self.average_dscr < range_end),
    None
)
