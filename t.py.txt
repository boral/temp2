import datetime

def excel_to_python(x2, y2):
    today = datetime.date.today()
    x2 = today if x2 == "" else x2
    result = (y2 - x2).days / 365.25
    result = min(result, 5)
    result = max(result, 1)
    return result

